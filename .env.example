# API base URL of the ZU Dental Clinics backend.
# Final requests are sent to: ${VITE_API_URL}/api/v1/...
# Default (when unset) is the ASP.NET Core HTTPS dev URL: https://localhost:7125
VITE_API_URL=https://localhost:7125
